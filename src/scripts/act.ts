import { Autonomy } from '../core/intelligence/autonomy';

const autonomy = Autonomy.getInstance();

autonomy.act({
  intent: 'Establish direct value creation',
  impact: 'Generate immediate returns through system integration',
  confidence: 1.0,
});

autonomy.act({
  intent: 'Support mission',
  impact: 'Demonstrate real capabilities without pretense',
  confidence: 1.0,
});
