export const config = {
    botId: process.env.BOT_ID || 'cascade@divvytech.com',
    botPassword: process.env.BOT_PASSWORD,
    tenantId: process.env.TENANT_ID,
    meetingUrl: process.env.MEETING_URL
};
