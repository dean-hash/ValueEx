interface ExponentialExecution {
  multipliers: {
    consciousness: string[];
    technology: string[];
    synergy: Map<string, number>;
  };
  practical_impact: {
    before: Map<string, number>;
    after: Map<string, number>;
    multiplier: number;
  };
  real_world: {
    immediate: string[];
    compound: Map<string, any>;
    acceleration: Set<string>;
  };
}
export declare class ExponentialManifestion {
  private field;
  private mission;
  accelerate(): Promise<ExponentialExecution>;
  manifest(): Promise<void>;
}
export {};
